<view class="{{['st-segment',b]}}">
  <view
    bindtap="{{item.c}}"
    class="{{['st-segment-item',item.b]}}"
    role="tab"
    wx:for="{{a}}"
    wx:key="d"
  >
    <view class="st-segment-item-inner"></view>{{item.a}}</view
  >
</view>
