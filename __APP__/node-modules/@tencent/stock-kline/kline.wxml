<view class="kline-comp">
  <canvas
    bindmousedown="{{k}}"
    bindmousemove="{{l}}"
    bindmouseup="{{m}}"
    bindmousewheel="{{n}}"
    bindtouchend="{{j}}"
    bindtouchmove="{{i}}"
    bindtouchstart="{{h}}"
    class="kline-content"
    disableScroll="{{g}}"
    height="{{d}}"
    id="{{a}}"
    ref="{{b}}"
    style="{{'width:'+e+';'+'height:'+f}}"
    type="2d"
    width="{{c}}"
    wx:if="{{r0}}"
  ></canvas>
  <canvas
    bindmousedown="{{A}}"
    bindmousemove="{{B}}"
    bindmouseup="{{C}}"
    bindmousewheel="{{D}}"
    bindtouchend="{{z}}"
    bindtouchmove="{{y}}"
    bindtouchstart="{{x}}"
    disableScroll="{{w}}"
    height="{{s}}"
    id="{{p}}"
    ref="{{q}}"
    style="{{'width:'+t+';'+'height:'+v+';'+'position:'+'absolute'+';'+'top:'+0}}"
    type="2d"
    width="{{r}}"
    wx:if="{{o}}"
  ></canvas>
  <slot></slot>
</view>
