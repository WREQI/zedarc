<view class="_div">
  <view class="_div" style="{{l}}" wx:if="{{a}}">
    <information
      bind:__l="__l"
      bindgetLogin="{{j}}"
      bindgotoColumnEditPage="{{i}}"
      bindhandleComment="{{d}}"
      bindhandleCommunityEdit="{{c}}"
      bindonMpScroll="{{g}}"
      bindpageInit="{{h}}"
      bindvideoCommentClick="{{f}}"
      bindvideoShareClick="{{e}}"
      class="r"
      uI="0a24f597-0"
      uP="{{k}}"
      uR="information"
      wx:if="{{k}}"
    ></information>
  </view>
  <comment-panel
    bind:__l="__l"
    bindclose="{{o}}"
    bindhandleGoEdit="{{q}}"
    bindonPutComment="{{r}}"
    bindsetCommentCount="{{p}}"
    class="r"
    uI="0a24f597-1"
    uP="{{s}}"
    uR="commentPanel"
    wx:if="{{m}}"
  ></comment-panel>
  <half-editor
    bind:__l="__l"
    bindonHide="{{w}}"
    class="r"
    uI="0a24f597-2"
    uP="{{x}}"
    uR="halfEditor"
    wx:if="{{t}}"
  ></half-editor>
  <privacy-policy-modal
    bind:__l="__l"
    bindinput="{{y}}"
    uI="0a24f597-3"
    uP="{{z}}"
    wx:if="{{z}}"
  ></privacy-policy-modal>
</view>
