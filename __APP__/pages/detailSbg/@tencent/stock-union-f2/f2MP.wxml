<view
  bindtouchend="{{events.touchend}}"
  bindtouchmove="{{events.touchmove}}"
  bindtouchstart="{{events.touchstart}}"
  class="{{['_div','f2-container',h]}}"
  data-displaytooltip="{{j}}"
  style="{{i}}"
>
  <canvas
    bindtouchcancel="{{g}}"
    bindtouchend="{{f}}"
    bindtouchmove="{{e}}"
    bindtouchstart="{{d}}"
    canvasId="{{b}}"
    class="{{['f2-canvas']}}"
    id="{{a}}"
    style="{{c}}"
    type="2d"
    wx:if="{{r0}}"
  ></canvas>
  <slot></slot>
</view>
