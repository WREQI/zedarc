<view
  catch:touchmove
  bindtap="{{d}}"
  capture-bind:touchmove="{{e}}"
  class="{{['_div','^mp-overlay','^^mp-overlay','^^^mp-overlay','mp-overlay',b]}}"
  hidden="{{!a}}"
  style="{{'z-index:'+c}}"
>
  <slot></slot>
</view>
