<half-screen-trade
  bind:__l="__l"
  class="r"
  uI="341aa70e-0"
  uP="{{aJ}}"
  uR="halfScreenTradeRef"
  uS="{{['d']}}"
  wx:if="{{aJ}}"
>
  <zhaoshang-embedded
    bindclose="{{l}}"
    bindmounted="{{k}}"
    class="embedded-stock"
    df_dqj="{{i}}"
    embedded="{{true}}"
    embeddedTradeMode="{{j}}"
    embedded_entrust_type="{{e}}"
    embedded_market="{{c}}"
    embedded_name="{{d}}"
    embedded_price="{{h}}"
    embedded_stock="{{b}}"
    embedded_stock_type="{{f}}"
    embedded_visible="{{g}}"
    key="zhaoshang-embedded"
    wx:if="{{a}}"
  ></zhaoshang-embedded>
  <hualin-embedded
    bindclose="{{y}}"
    bindmounted="{{x}}"
    class="embedded-stock"
    df_dqj="{{t}}"
    embedded="{{true}}"
    embeddedTradeMode="{{w}}"
    embedded_entrust_type="{{q}}"
    embedded_market="{{o}}"
    embedded_name="{{p}}"
    embedded_price="{{v}}"
    embedded_stock="{{n}}"
    embedded_stock_type="{{r}}"
    embedded_visible="{{s}}"
    key="hualin-embedded"
    wx:if="{{m}}"
  ></hualin-embedded>
  <guoxin-embedded
    bindclose="{{K}}"
    bindmounted="{{J}}"
    class="embedded-stock"
    df_dqj="{{F}}"
    embedded="{{true}}"
    embeddedTradeMode="{{I}}"
    embedded_entrust_type="{{D}}"
    embedded_market="{{B}}"
    embedded_name="{{C}}"
    embedded_price="{{G}}"
    embedded_stock="{{A}}"
    embedded_stock_type="{{H}}"
    embedded_visible="{{E}}"
    key="guoxin-embedded"
    wx:if="{{z}}"
  ></guoxin-embedded>
  <guojin-embedded
    bindclose="{{W}}"
    bindmounted="{{V}}"
    class="embedded-stock"
    df_dqj="{{R}}"
    embedded="{{true}}"
    embeddedTradeMode="{{U}}"
    embedded_entrust_type="{{P}}"
    embedded_market="{{N}}"
    embedded_name="{{O}}"
    embedded_price="{{S}}"
    embedded_stock="{{M}}"
    embedded_stock_type="{{T}}"
    embedded_visible="{{Q}}"
    key="guojin-embedded"
    wx:if="{{L}}"
  ></guojin-embedded>
  <zhongjincaifu-embedded
    bindclose="{{ai}}"
    bindmounted="{{ah}}"
    class="embedded-stock"
    df_dqj="{{ad}}"
    embedded="{{true}}"
    embeddedTradeMode="{{ag}}"
    embedded_entrust_type="{{ab}}"
    embedded_market="{{Z}}"
    embedded_name="{{aa}}"
    embedded_price="{{ae}}"
    embedded_stock="{{Y}}"
    embedded_stock_type="{{af}}"
    embedded_visible="{{ac}}"
    key="zhongjincaifu-embedded"
    wx:if="{{X}}"
  ></zhongjincaifu-embedded>
  <zhongxinjiantou-embedded
    bindclose="{{av}}"
    bindmounted="{{at}}"
    class="embedded-stock"
    df_dqj="{{ap}}"
    embedded="{{true}}"
    embeddedTradeMode="{{as}}"
    embedded_entrust_type="{{an}}"
    embedded_market="{{al}}"
    embedded_name="{{am}}"
    embedded_price="{{aq}}"
    embedded_stock="{{ak}}"
    embedded_stock_type="{{ar}}"
    embedded_visible="{{ao}}"
    key="zhongxinjiantou-embedded"
    wx:if="{{aj}}"
  ></zhongxinjiantou-embedded>
  <guangfa-embedded
    bindclose="{{aH}}"
    bindmounted="{{aG}}"
    class="embedded-stock"
    df_dqj="{{aC}}"
    embedded="{{true}}"
    embeddedTradeMode="{{aF}}"
    embedded_entrust_type="{{aA}}"
    embedded_market="{{ay}}"
    embedded_name="{{az}}"
    embedded_price="{{aD}}"
    embedded_stock="{{ax}}"
    embedded_stock_type="{{aE}}"
    embedded_visible="{{aB}}"
    key="guangfa-embedded"
    wx:if="{{aw}}"
  ></guangfa-embedded>
</half-screen-trade>
