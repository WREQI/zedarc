<view class="{{['_div','live-nav-bar',r]}}">
  <live-common-bar
    bind:__l="__l"
    bindonClick="{{b}}"
    bindonClose="{{c}}"
    uI="143f3c5f-0"
    uP="{{d}}"
    wx:if="{{a}}"
  ></live-common-bar>
  <live-common-bar
    bind:__l="__l"
    bindonClick="{{f}}"
    bindonClose="{{g}}"
    uI="143f3c5f-1"
    uP="{{h}}"
    wx:elif="{{e}}"
  ></live-common-bar>
  <subscribe-wx-msg-bar
    bind:__l="__l"
    bindcloseSubscribeBar="{{k}}"
    bindexplore="{{l}}"
    bindonSubscribe="{{j}}"
    class="subscribe-wxmsg-bar"
    uI="143f3c5f-2"
    uP="{{m}}"
    wx:elif="{{i}}"
  ></subscribe-wx-msg-bar>
  <live-common-bar
    bind:__l="__l"
    bindonClick="{{o}}"
    bindonClose="{{p}}"
    uI="143f3c5f-3"
    uP="{{q}}"
    wx:elif="{{n}}"
  ></live-common-bar>
</view>
