<view class="password-component__wrapper" hidden="{{!z}}">
  <view wx:if="{{a}}">
    <embedded
      bind:__l="__l"
      bindcancel="{{f}}"
      binderror="{{g}}"
      bindhide="{{d}}"
      bindpwdReset="{{h}}"
      bindsuccess="{{e}}"
      class="r"
      uI="cc71a904-0"
      uP="{{i}}"
      uR="password"
      wx:if="{{b}}"
    ></embedded>
    <embedded-complex
      bind:__l="__l"
      bindcancel="{{m}}"
      binderror="{{n}}"
      bindhide="{{k}}"
      bindpwdReset="{{o}}"
      bindsuccess="{{l}}"
      class="r"
      uI="cc71a904-1"
      uP="{{p||''}}"
      uR="password"
      wx:else
    ></embedded-complex>
  </view>
  <view wx:if="{{q}}">
    <bank
      bind:__l="__l"
      bindcancel="{{v}}"
      binderror="{{w}}"
      bindhide="{{s}}"
      bindpwdReset="{{x}}"
      bindsuccess="{{t}}"
      class="r"
      uI="cc71a904-2"
      uP="{{y}}"
      uR="password"
      wx:if="{{y}}"
    ></bank>
  </view>
</view>
