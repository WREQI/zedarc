<view class="global-adapt-theme" data-simple="{{d}}">
  <password-component
    bind:__l="__l"
    class="r"
    id="password-component"
    uI="58e87611-0"
    uP="{{b}}"
    uR="#password-component"
    wx:if="{{b}}"
  ></password-component>
  <mp-dialog
    bind:__l="__l"
    id="mp-dialog"
    uI="58e87611-1"
    uP="{{c}}"
    wx:if="{{c}}"
  ></mp-dialog>
</view>
