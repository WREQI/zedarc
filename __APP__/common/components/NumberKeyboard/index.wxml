<view class="{{['st-number-keyboard',v]}}" hidden="{{!t}}">
  <view catchtouchmove="{{s}}" class="{{r}}">
    <view class="st-number-keyboard__title" wx:if="{{a}}">
      <text
        bindtap="{{c}}"
        class="icon icon-close st-number-keyboard__close"
        wx:if="{{b}}"
      ></text>
      <text>{{d}}</text>
      <text bindtap="{{f}}" class="st-number-keyboard__done" wx:if="{{e}}"
        >完成</text
      >
    </view>
    <slot name="keyboard-top"></slot>
    <view class="st-number-keyboard__body">
      <view class="st-number-keyboard__keys">
        <block wx:if="{{g}}">
          <block wx:for="{{h}}" wx:for-item="key" wx:key="k">
            <text
              catchtap="{{key.d}}"
              catchtouchend="{{key.f}}"
              catchtouchstart="{{key.e}}"
              class="{{['st-number-key','^st-number-key',key.b,key.c]}}"
              role="NumberKey"
              wx:if="{{i}}"
              >{{key.a}}</text
            >
            <text
              catchtap="{{key.j}}"
              class="{{['st-number-key',key.h,key.i]}}"
              role="NumberKey"
              wx:else
              >{{key.g}}</text
            >
          </block>
        </block>
        <block wx:else>
          <key
            bind:__l="__l"
            bindpress="{{key.b}}"
            class="st-key__wrapper"
            uI="{{key.c}}"
            uP="{{key.d}}"
            wx:for="{{j}}"
            wx:for-item="key"
            wx:key="a"
          ></key>
        </block>
      </view>
      <view class="st-number-keyboard__sidebar" wx:if="{{k}}">
        <key
          bind:__l="__l"
          bindpress="{{l}}"
          class="st-key__wrapper"
          uI="40efdf50-1"
          uP="{{m}}"
          wx:if="{{m}}"
        ></key>
        <key
          bind:__l="__l"
          bindpress="{{n}}"
          class="st-key__wrapper"
          uI="40efdf50-2"
          uP="{{o}}"
          wx:if="{{o}}"
        ></key>
        <key
          bind:__l="__l"
          bindpress="{{p}}"
          class="st-key__wrapper"
          uI="40efdf50-3"
          uP="{{q}}"
          wx:if="{{q}}"
        ></key>
      </view>
    </view>
  </view>
</view>
