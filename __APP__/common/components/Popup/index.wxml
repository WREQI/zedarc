<view>
  <overlay
    bind:__l="__l"
    bindclick="{{b}}"
    uI="64baed7c-0"
    uP="{{c}}"
    wx:if="{{a}}"
  ></overlay>
  <view
    class="{{['^mp-popup','^^mp-popup','^^^mp-popup','mp-popup',e,f]}}"
    hidden="{{!d}}"
    style="{{g}}"
  >
    <slot></slot>
  </view>
</view>
