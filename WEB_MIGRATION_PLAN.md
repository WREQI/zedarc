# 微信小程序改造为 Vue Web 应用总体方案与计划

## 一、总体目标

将现有微信小程序逐步改造成一个可在浏览器运行的 Web 应用。

整体分为四个阶段：

1. **第一阶段：静态 Web 页面 MVP**
   - 浏览器可以直接访问
   - 可以切换主要页面
   - 页面结构、视觉风格和交互方式方便验收
   - 使用本地 mock 数据
   - 暂不接入真实接口、登录和交易能力

2. **第二阶段：主要业务数据接入**
   - 接入行情、资讯、自选股等接口
   - 实现搜索、筛选、分页、刷新等交互
   - 建立统一的数据请求和错误处理机制

3. **第三阶段：用户体系和业务能力迁移**
   - 登录与用户状态
   - 自选股同步
   - 评论、消息、收藏
   - 真实 K 线
   - 券商交易跳转
   - 微信专属能力的 Web 替代方案

4. **第四阶段：全量页面迁移和上线优化**
   - 按业务模块补齐剩余页面
   - PC、平板、移动端适配
   - 性能优化、SEO、监控和部署

---

## 二、项目现状评估

当前小程序根据 `app.json` 路由配置统计：

- 主包页面：6 个
- 分包数量：53 个
- 分包页面：551 个
- 注册页面合计：**557 个**

目录中的大量 WXML、JavaScript 和组件文件还包含组件、插件、编译产物以及微信专属页面，因此文件数量不等于实际业务页面数量。

项目中还包含以下微信专属能力：

- 微信验证码
- 腾讯视频插件
- AI 语音插件
- 多家券商交易插件
- WebView 页面
- 微信登录、授权、分享等能力

这些功能无法直接复制到 Web，需要单独设计替代方案。

---

## 三、总体技术方案

### 1. 推荐技术栈

```text
Vue 3
TypeScript
Vite
Vue Router
Pinia
Element Plus 或 Naive UI
ECharts / Lightweight Charts
SCSS 或 Tailwind CSS
```

第一阶段建议使用：

```text
Vue 3 + TypeScript + Vite + Vue Router + Pinia
```

第一阶段暂不引入 Nuxt，优先保证开发速度和项目结构轻量。

### 2. 新旧项目关系

不建议直接将 Web 代码混入当前小程序目录，建议在 `zedarc` 根目录下创建独立 Web 工程。旧小程序位于 `__APP__` 目录，Web 项目位于同级的 `web` 目录：

```text
zedarc/
├── __APP__/                 # 旧微信小程序项目
│   └── 原小程序代码
└── web/                     # 新 Vue Web 项目，与 __APP__ 同级
    ├── package.json
    ├── vite.config.ts
    ├── src/
    └── public/
```

也就是说，两个项目的目录关系为：

```text
zedarc/__APP__/
zedarc/web/
```

Web 项目的所有开发、依赖安装和构建命令均在 `zedarc/web` 目录下执行，不要将 Web 代码放入 `zedarc/__APP__/web`。

这样可以：

- 保留原小程序作为参考和对照
- 避免破坏现有项目
- 方便新旧页面逐个比对
- 让 Web 版本独立开发和部署
- 支持小程序与 Web 并行维护

### 开发预览原则

Web 项目采用“先运行、再迭代”的方式，不等待全部页面重构完成：

1. `zedarc/web` 工程创建完成后，优先启动 Vue + Vite 开发服务器。
2. 先显示一个可访问的 Vue 首页和全局布局，即使业务内容仍使用占位或 mock 数据。
3. 后续每完成一个组件、路由或页面，都立即在浏览器中查看效果并进行调整。
4. 使用 Vite HMR（Hot Module Replacement，热更新）实现 AI 修改代码后的实时刷新；通常保存文件后浏览器会自动更新，无需重新构建整个项目。
5. 旧小程序继续作为页面和业务逻辑参考，不阻塞 Web 项目的启动和渐进式迁移。

推荐开发命令：

```bash
cd zedarc/web
npm install
npm run dev
```

开发地址以终端输出为准，默认通常为 `http://localhost:5173`。

---

## 四、页面迁移策略

不建议一开始逐个迁移 557 个页面，应按照业务价值分层处理。

### A 类：第一阶段优先迁移页面

建议优先实现 10～15 个核心页面：

1. 首页
2. 行情首页
3. 股票列表
4. 股票搜索
5. 自选股
6. 股票详情
7. K 线页面
8. 资讯列表
9. 资讯详情
10. 研报列表
11. 个人中心
12. 消息中心
13. 设置页面
14. 登录占位页
15. 全局错误页和空状态页

### B 类：第二阶段迁移页面

预计 30～60 个页面：

- 市场板块
- ETF
- 龙虎榜
- 行业排行
- 资金流向
- 专题资讯
- 研报详情
- 历史浏览
- 股票篮子
- 活动页面
- 评论和互动页面
- 更多账户页面

### C 类：后续按需迁移页面

根据访问频率和业务优先级继续迁移：

- 微信插件页面
- 券商交易页面
- WebView 页面
- 活动落地页
- 低频配置页
- 空状态、异常状态和中间跳转页面

---

# 五、第一阶段：静态 Web 页面 MVP

## 1. 阶段目标

第一阶段的目标不是完成全部业务，而是：

> 在浏览器中搭建一个可点击、可浏览、可验收的 Web 静态原型。

验收人员应可以通过浏览器看到：

- Web 端整体布局
- 页面导航关系
- 主要页面视觉效果
- 主要组件形态
- 移动端和桌面端适配效果
- 基本交互反馈

## 2. 第一阶段页面范围

### 全局布局

实现：

- 顶部导航栏
- 左侧或底部主导航
- 内容区布局
- 响应式布局
- 页面标题区
- 返回按钮
- 面包屑或页面路径
- 全局加载状态
- 全局空状态
- 全局错误状态

### 首页

使用 mock 数据展示：

- 市场概览
- 指数行情
- 热门股票
- 涨幅榜
- 跌幅榜
- 最新资讯
- 自选股摘要
- 快速搜索入口

### 行情列表

实现：

- 行情 Tab
- 市场切换
- 排序
- 涨跌幅展示
- 股票列表
- 行业标签
- 分页或加载更多
- 空数据状态

### 自选股

实现：

- 自选股列表
- 分组 Tab
- 股票状态
- 添加和删除按钮
- 行情卡片
- 无自选股状态

第一阶段的添加和删除可以只保存到浏览器本地状态。

### 股票详情

实现：

- 股票名称和代码
- 当前价格
- 涨跌额和涨跌幅
- 开高低收等基础指标
- 分时图占位或静态图
- K 线图
- 成交量
- 技术指标 Tab
- 盘口数据
- 相关资讯

第一阶段图表只要求视觉和基本交互可用，不接入实时行情。

### 搜索

实现：

- 搜索框
- 搜索历史
- 热门搜索
- 股票搜索结果
- 资讯搜索结果
- 无搜索结果状态

### 资讯和研报

实现：

- 资讯列表
- 资讯分类
- 资讯详情
- 研报列表
- 研报详情
- 收藏按钮占位
- 分享按钮占位

### 个人中心

实现：

- 用户信息占位
- 账户入口
- 浏览历史
- 消息中心入口
- 设置入口
- 登录状态和未登录状态
- 联系客服等功能入口占位

---

# 六、第一阶段实施步骤

## Step 0：项目和需求基线确认

### 目标

在正式写页面前，确认页面范围和视觉方向。

### 工作内容

- 整理现有小程序页面路由
- 确认第一批 10～15 个页面
- 确认 PC 端和移动端优先级
- 确认是否尽量复刻小程序视觉
- 确认 Web 端是否允许重新设计布局
- 选取典型页面作为视觉基准

### 产出

```text
页面清单
页面优先级
页面跳转关系
视觉参考页面
第一阶段验收范围
```

预计：**0.5～1 天**

## Step 1：搭建 Vue Web 基础工程

### 目标

先让浏览器立即显示 Vue 页面，建立可以持续预览和迭代的开发环境，而不是等页面全部重构完成后再启动。

### 工作内容

- 在 `zedarc/web` 创建 Vue 3 + TypeScript + Vite 工程
- 配置 ESLint、Prettier
- 配置 Vue Router
- 配置 Pinia
- 配置环境变量
- 配置基础样式和全局布局
- 配置路径别名
- 创建一个可访问的 Vue 首页或欢迎页
- 配置开发启动命令和 Vite HMR 热更新
- 验证保存 `.vue`、`.ts` 或样式文件后浏览器可以实时更新

### 产出

Step 1 完成后即可打开浏览器查看 Vue 页面：

```bash
cd zedarc/web
npm install
npm run dev
```

开发地址以终端输出为准，默认通常为：

```text
http://localhost:5173
```

此时页面可以只有基础布局和占位内容，但必须能够运行、刷新和实时预览。

预计：**0.5～1 天**

## Step 2：建立设计系统和基础组件

### 目标

先解决全站统一性，避免每个页面单独开发。

### 基础组件建议

```text
AppLayout
TopHeader
SideNavigation
MobileNavigation
PageHeader
SearchInput
Tabs
StockPrice
StockTable
StockCard
NewsCard
ChartPanel
FilterBar
Modal
Drawer
Badge
EmptyState
LoadingState
ErrorState
Pagination
```

### 统一内容

- 颜色
- 字体
- 间距
- 圆角
- 边框
- 行高
- 涨跌颜色
- 响应式断点
- 按钮状态
- 输入框状态
- 表格行高
- 统一数字字体

### 产出

基础组件演示页：

```text
/components
```

预计：**1～2 天**

## Step 3：建立页面路由和 mock 数据

### 目标

先建立路由骨架和 mock 数据，并保证每新增一个页面都可以立即访问和预览。不要等待所有页面内容完成后再统一接入。

### 推荐目录

```text
src/
├── components/
├── layouts/
├── pages/
│   ├── HomePage.vue
│   ├── MarketPage.vue
│   ├── WatchlistPage.vue
│   ├── StockSearchPage.vue
│   ├── StockDetailPage.vue
│   ├── NewsListPage.vue
│   ├── NewsDetailPage.vue
│   ├── ReportListPage.vue
│   ├── AccountPage.vue
│   └── SettingsPage.vue
├── router/
├── stores/
├── mock/
│   ├── market.ts
│   ├── stocks.ts
│   ├── news.ts
│   └── user.ts
├── services/
└── styles/
```

### Mock 数据原则

mock 数据应尽量接近未来接口结构，以便第二阶段替换真实数据时不需要大规模重写页面。

示例：

```ts
export interface StockQuote {
  code: string
  name: string
  price: number
  change: number
  changePercent: number
  volume: number
}
```

预计：**0.5～1 天**

## Step 4：按垂直切片实现核心页面

每个页面都按照“路由 → 页面骨架 → mock 数据 → 交互 → 响应式 → 浏览器验收”的顺序完成。每完成一个切片，就在运行中的开发页面中检查效果，再进入下一个页面；不要求等全部页面完成后统一验收。

建议按照以下顺序实现：

### 4.1 首页和全局导航

- 首页
- 顶部导航
- 侧边导航
- 移动端底部导航
- 页面跳转

### 4.2 行情和自选股

建立以下通用能力：

- 股票表格
- 股票卡片
- 涨跌数据
- 筛选栏
- Tab
- 排序交互

### 4.3 股票详情和 K 线

- 详情头部
- 图表区域
- 指标区域
- 盘口区域
- 资讯区域

### 4.4 资讯和研报

- 分类 Tab
- 新闻卡片
- 详情排版
- 推荐内容

### 4.5 个人中心和设置

- 用户状态
- 功能入口
- 设置项
- 登录占位

每完成一个页面切片就进行一次浏览器预览和快速验收。核心页面全部完成后，再做一次全局一致性检查。

预计：**4～7 天**

## Step 5：持续完成交互和响应式适配

交互和响应式不是最后才开始的工作，而是随每个页面切片同步完成。AI 修改页面后保持开发服务器运行，通过 Vite HMR 立即观察桌面端和移动端宽度下的变化。

### 第一阶段交互范围

- 页面跳转
- Tab 切换
- 搜索过滤
- 列表排序
- 分页
- 添加和删除自选股
- 弹窗打开和关闭
- 抽屉打开和关闭
- 收藏状态切换
- 浏览器本地存储
- 返回上一页
- 移动端导航切换

### 第一阶段暂不实现

- 真实登录
- 真实行情刷新
- 交易下单
- 支付
- 微信授权
- 视频播放
- AI 语音
- 券商插件
- 服务端搜索

预计：**贯穿 Step 3～Step 4，集中收尾 1～2 天**

## Step 6：验收和调整

### 验收内容

- 页面是否可以正常打开
- 路由是否正确
- 主要跳转是否完整
- 页面是否存在明显错位
- PC 端是否正常
- 移动端是否可用
- 文字是否溢出
- 表格是否支持横向滚动
- 涨跌颜色是否统一
- 空状态和加载状态是否存在
- 浏览器刷新后路由是否正常
- 控制台是否有明显报错

预计：**1～2 天**

---

# 七、第一阶段时间计划

以单人开发、GPT 辅助、现有资源可使用为前提：

| 阶段 | 内容 | 时间 |
|---|---|---:|
| Step 0 | 需求和页面范围确认 | 0.5～1 天 |
| Step 1 | Vue 工程搭建并立即启动预览 | 0.5～1 天 |
| Step 2 | 设计系统和基础组件（边做边预览） | 1～2 天 |
| Step 3 | 路由和 mock 数据 | 0.5～1 天 |
| Step 4 | 按页面切片开发核心页面 | 4～7 天 |
| Step 5 | 随页面同步完成交互和响应式 | 贯穿 Step 3～Step 4，收尾 1～2 天 |
| Step 6 | 验收修复 | 1～2 天 |
| **合计** | **第一阶段静态 Web MVP** | **8～14 个工作日** |

如果只做一个快速验收版本，先完成以下页面：

- 首页
- 行情列表
- 自选股
- 股票详情
- 资讯列表
- 个人中心

预计：**3～5 个工作日**。

---

# 八、第一阶段验收标准

## 页面层面

- Step 1 完成后即可看到可运行的 Vue 页面
- 至少完成 10 个核心页面
- 每个页面有明确 URL
- 页面可以通过导航互相跳转
- 每个页面或组件修改后可以在开发服务器中即时预览
- 页面刷新后不会出现空白
- 页面可以在桌面浏览器打开
- 页面可以在移动端宽度下正常显示

## 数据层面

- 使用本地 mock 数据
- 数据结构与未来接口保持一致
- 股票、行情、资讯、用户信息都有示例数据
- 涨跌数据和空数据状态都覆盖

## 交互层面

- 搜索可以过滤 mock 数据
- Tab 可以切换
- 自选股可以添加和删除
- 弹窗和抽屉可以正常打开关闭
- 页面返回逻辑正常
- 基本状态有视觉反馈

## 视觉层面

- 全站颜色和字体统一
- 涨跌颜色统一
- 表格和卡片样式统一
- PC 和移动端布局清晰
- 没有明显的占位符、乱码或断裂布局
- 不依赖小程序 API 才能显示页面

## 工程层面

- 在 `zedarc/web` 执行 `npm install` 可以安装依赖
- 在 `zedarc/web` 执行 `npm run dev` 可以启动开发服务器
- Vite HMR 可以在保存代码后实时更新浏览器页面
- 在 `zedarc/web` 执行 `npm run build` 可以构建
- 没有明显 TypeScript 错误
- 没有明显控制台报错
- 页面组件可以复用
- mock 数据和页面代码分离

---

# 九、第二阶段：接入真实数据

## 目标

将静态页面替换为真实业务数据。

## 工作内容

1. 梳理现有接口调用
2. 建立 Web API 请求层
3. 统一请求参数和返回结构
4. 接入行情数据
5. 接入资讯和研报接口
6. 接入搜索接口
7. 实现登录状态
8. 实现自选股同步
9. 增加 loading、error、retry 状态
10. 处理权限和接口异常

## 预估

**2～4 周**，具体取决于：

- 是否已有 Web 可用接口
- 接口是否依赖微信登录态
- 行情是否需要 WebSocket
- 后端是否允许跨域
- 是否需要重新设计鉴权方案

---

# 十、第三阶段：微信能力替代

| 小程序能力 | Web 替代方案 |
|---|---|
| 微信登录 | 手机号登录、短信登录、OAuth 登录 |
| 小程序分享 | Web Share API、复制链接 |
| 微信支付 | H5 支付或第三方支付 |
| 券商插件 | 券商 H5、WebView 或跳转 App |
| 腾讯视频插件 | Web 视频播放器 |
| AI 语音插件 | Web Speech API 或云端语音服务 |
| 小程序 WebView | iframe、外链或页面重构 |
| 微信授权 | 浏览器权限机制或业务自建授权 |
| 小程序本地缓存 | localStorage / IndexedDB |
| `wx.request` | fetch / axios |
| 小程序导航 | Vue Router |

---

# 十一、推荐的实际执行方式

建议采用三次验收：

## 验收 1：框架验收

### 目标

- Vue 工程运行
- 全局布局完成
- 导航完成
- 首页骨架完成
- 基础颜色和字体确定

### 时间

第 **2～3 天**。

## 验收 2：核心页面验收

### 目标

以下页面全部可以访问和交互：

- 首页
- 行情
- 自选股
- 股票详情
- 资讯
- 个人中心

### 时间

第 **5～8 天**。

## 验收 3：静态 MVP 验收

### 目标

- 10～15 个页面完成
- 页面路由闭环
- 响应式完成
- mock 数据完整
- 基础交互完成
- 构建和部署验证通过

### 时间

第 **8～14 天**。

---

# 十二、最终执行路线

```text
盘点页面
  ↓
建立 zedarc/web Vue Web 工程
  ↓
立即启动 Vite，先显示 Vue 首页和全局布局
  ↓
AI 修改代码，浏览器通过 HMR 实时预览
  ↓
确定 10～15 个核心页面
  ↓
按页面切片逐个完成：路由 → 骨架 → mock → 交互 → 响应式
  ↓
每完成一个切片立即浏览器验收
  ↓
形成可点击的静态 Web MVP
  ↓
接入真实接口
  ↓
处理登录、交易和微信能力
  ↓
按业务优先级扩展剩余页面
```

第一阶段应明确控制为：

> 不是一次性迁移 557 个页面，也不是等全部重构完才启动 Web；而是先让 `zedarc/web` 运行起来，再以可预览的页面切片持续迭代，最终形成可运行、可点击、可评审的 Vue 静态 Web MVP。

建议第一阶段按 **8～14 个工作日** 规划，首个可验收版本按 **3～5 个工作日** 规划。
